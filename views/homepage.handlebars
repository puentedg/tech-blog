<div class="container">
    <div class="columns">
    <div class="column is-half">
        <p>Posts</p>
        <div>
        {{#each posts as |post| }}
        {{> post-details}}
        {{/each}}
        </div>
    </div>
    </div>
</div>
<script src="/js/homepage.js"></script>
